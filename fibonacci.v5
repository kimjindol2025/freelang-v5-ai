// FreeLang v5 - Fibonacci 프로그램
// 함수형 프로그래밍

fn fib(n: number) -> number {
  if (n <= 1) return n;
  return fib(n - 1) + fib(n - 2);
}

fn factorial(n: number) -> number {
  if (n <= 1) return 1;
  return n * factorial(n - 1);
}

fn isEven(n: number) -> boolean {
  return n % 2 == 0;
}
